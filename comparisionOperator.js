// Loose equals to ( == )
console.log(2 == "2") // It checks the equality of values but not the data type


// Strict equals to ( === )

console.log(2 === "2") // It checks the equality and data type  
